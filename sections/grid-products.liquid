{%- comment -%}
------------------------------------------------------------------------------
	Section: Grid Products
------------------------------------------------------------------------------
{%- endcomment -%}

{%- style -%}
	#shopify-section-{{- section.id -}} {
		padding-top: {{- section.settings.offset_top_mobile -}}px;
		padding-bottom: {{- section.settings.offset_bottom_mobile -}}px;
	}

	@media screen and (min-width: 768px) {
	#shopify-section-{{- section.id -}} {
			padding-top: {{- section.settings.offset_top_desktop -}}px;
			padding-bottom: {{- section.settings.offset_bottom_desktop -}}px;
		}
	}
{%- endstyle -%}

{%- assign container 			= section.settings.container_width -%}
{%- assign blocks 				= section.blocks  -%}
{%- assign has_filled_block 	= true -%}

{%- if container == 'inherit' -%}
	{%- assign container_width = settings.container_width -%}
{%- else -%}
	{% assign container_width = container %}
{%- endif -%}

{%- assign image_size        = 640 -%}
{%- assign image_size_tablet = 382 -%}
{%- assign image_size_mobile = 240 -%}


{%- assign heading 					= section.settings.heading -%}

{%- capture style -%}
	--container-width: {{ container_width -}};
{%- endcapture -%}

{%- if has_filled_block -%}
	<section class="products-thumbnails js-products-thumbnails-section js-animate {% if request.visual_preview_mode or section.index == 1 %}animated{% endif %}"
			 style="{{- style -}}"
	>
		{%- render 'adastra' with '/src/styles/products-thumbnails.scss' -%}

		<div class="products-thumbnails__container">
			<div class="products-thumbnails__heading-wrapper">
				{%- if heading != blank -%}
					<h2 class="product-tabs__title need-animate tr-dur-0-5">{{- heading -}}</h2>
				{%- endif -%}

				{%- assign product_list = section.settings.product_list -%}
				{%- assign button_label = section.settings.button_label -%}
				{%- assign button_link = section.settings.button_link -%}


					<div
						class="products-thumbnails__tab js-products-thumbnails-tab"
						data-index="{{- index -}}">
			
							<div class="products-thumbnails__tab-content products-thumbnails__tab-content--grid">
								{%- if product_list.count > 0 -%}
									{% if forloop.index == 1 %}
										{% assign delay_card_step = 0.25 %}
									{% endif %}

									{%- for product in product_list limit: limit -%}
										{% assign delay_card_step = delay_card_step | plus: 0.25 %}
										<div class="products-thumbnails__product products-thumbnails__product--variant-card  products-thumbnails__product--{{ section.settings.items_per_row }}"
											 {% if forloop.parentloop.index == 1 %}style="--card-delay: {{ delay_card_step | append: 's' }};"{% endif %}
										>
											{%- render
												'product-card-form-variant',
												product: product,
												size: image_size,
												tablet_size: image_size_tablet,
												mobile_size: image_size_mobile,
											-%}
										</div>
									{%- endfor -%}
								{%- else -%}
									{%- for i in (1..4) -%}
										<div class="products-thumbnails__product products-thumbnails__product--variant-card products-thumbnails__product--{{ section.settings.items_per_row }}" ">
											{%- render 'product-card', product: nil -%}
										</div>
									{%- endfor -%}
								{%- endif -%}
							</div>
				

						{% if button_link != blank and button_label != blank %}
							<div class="products-thumbnails__button-wrapper need-animate tr-dur-0-5 tr-del-1-5">
								<a class="products-thumbnails__button button button--secondary-border"
								   href="{{ button_link }}">{{ button_label }}</a>
							</div>
						{% endif %}
					</div>
			</div>

	
		</div>

		<script src="{{ 'add-to-cart-grid.js' | asset_url }}" type="module" crossorigin="anonymous"></script>
	</section>
{%- endif -%}


{%- schema -%}
{
	"name": "Grid Products",
	"disabled_on": {
		"groups": [
			"header",
			"footer"
		]
	},
	"settings": [
		{
			"type": "text",
			"id": "heading",
			"label": "Heading",
			"default": "Grid Products"
		},
		{
			"type": "header",
			"content": "Layout"
		},
        {
            "type": "product_list",
            "id": "product_list",
            "label": "Products"
        },
        {
            "type": "text",
            "id": "button_label",
            "label": "Button label",
            "default": "See all products"
        },
        {
            "type": "url",
            "id": "button_link",
            "label": "Button link"
        },
        {
            "type": "select",
            "id": "items_per_row",
            "label": "Items per row",
            "options": [
                {
                    "value": "grid-1",
                    "label": "1 per row"
                },
                {
                    "value": "grid-2",
                    "label": "2 per row"
                },
                {
                    "value": "grid-3",
                    "label": "3 per row"
                },
                {
                    "value": "grid-4    ",
                    "label": "4 per row"
                }
        ],
        "default": "grid-2"
        },
		{
			"type": "select",
			"id": "container_width",
			"label": "Container width",
			"options": [
				{
					"value": "1432px",
					"label": "Default - 1400px"
				},
				{
					"value": "1952px",
					"label": "Large - 1920px"
				},
				{
					"value": "100%",
					"label": "Full width"
				},
				{
					"value": "inherit",
					"label": "Inherit of general setting"
				}
			],
			"default": "inherit",
			"info": "if you select 'Inherit of general setting', then container width will be used from the Theme settings/Layout"
		},
		{
			"type": "range",
			"id": "offset_top_desktop",
			"min": 0,
			"max": 100,
			"step": 1,
			"unit": "px",
			"label": "Desktop offset top",
			"default": 40
		},
		{
			"type": "range",
			"id": "offset_bottom_desktop",
			"min": 0,
			"max": 100,
			"step": 1,
			"unit": "px",
			"label": "Desktop offset bottom",
			"default": 40
		},
		{
			"type": "range",
			"id": "offset_top_mobile",
			"min": 0,
			"max": 100,
			"step": 1,
			"unit": "px",
			"label": "Mobile offset top",
			"default": 25
		},
		{
			"type": "range",
			"id": "offset_bottom_mobile",
			"min": 0,
			"max": 100,
			"step": 1,
			"unit": "px",
			"label": "Mobile offset bottom",
			"default": 25
		}
	],
	"presets": [
		{
			"name": "Grid Products"
		}
	]
}
{%- endschema -%}
